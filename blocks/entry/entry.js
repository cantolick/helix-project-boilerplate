export default function enhanceContent(block){if(!block)return;const headings=block.querySelectorAll('h2, h3, h4');headings.forEach((heading)=>{if(heading.tagName.toLowerCase()==='h2'){heading.classList.add('main-heading');}else if(heading.tagName.toLowerCase()==='h3'){heading.classList.add('sub-heading');}else if(heading.tagName.toLowerCase()==='h4'){heading.classList.add('date');const textContent=heading.textContent.trim();const date=new Date(textContent);if(!Number.isNaN(date.getTime())){const isoDate=date.toISOString().split('T')[0];const timeElement=document.createElement('time');timeElement.setAttribute('datetime',isoDate);timeElement.textContent=textContent;timeElement.classList.add('date');heading.replaceWith(timeElement);}}});}